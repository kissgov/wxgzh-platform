# V2.0 S3 → S3 启动交接 (Handoff)

> 写于 2026-06-02 S2 完工时
> 给新会话用:读完此文件 + S3 plan 即可启动

---

## 1. 当前 Git 状态 (截至 2026-06-02)

- **S2 完工 branch**: `v2-foundation/s2` @ `cd5e19b`
- **worktree 路径**: `i:\VsCodeWorkPlaces\Wxgzh\.worktrees\v2-foundation-s2`
- **未推 origin** (跟 S1 一致,推/合时机由用户决定)
- **main HEAD**: `f09b4f4` (V1 起点,未含 S1/S2)

S1 完工位置: `v2-foundation/s1` @ `af07dcc` (worktree: `i:\VsCodeWorkPlaces\Wxgzh\.worktrees\v2-foundation-s1`)

S2 = 21 commits (S1 之上): ZodValidationPipe + 3 装饰器 + 15 controller Zod 迁移 + HttpExceptionFilter 透出 errors + OpenAPI snapshot + ESLint no-body-any + CI gate。

**S3 启动前提**: S2 在 main 上 (即 `v2-foundation/s2` 已 merge 到 main)。如未合并,S3 应基于 `v2-foundation/s2` 开 worktree。

---

## 2. S3 Plan 入口

**Plan**: `docs/superpowers/plans/2026-06-02-v2-foundation-s3-observability.md`

主题:**可观测性 (OTel + Prometheus + pino + Grafana 4 看板 + 告警)**

依赖: S1 (测试底座) + S2 (DTO/Zod) — 两者已完成, S3 可直接开。

---

## 3. 启动 S3 的具体步骤

### 3.1 开 worktree (两种情况)

**情况 A: S2 已 merge 到 main**:
```bash
cd i:\VsCodeWorkPlaces\Wxgzh
git pull
git worktree add .worktrees/v2-foundation-s3 -b v2-foundation/s3 main
cd .worktrees/v2-foundation-s3
pnpm install
pnpm prisma generate  # 如果 prisma schema 变更
cd apps/server && pnpm test  # 应 ≥ 155/155 PASS
```

**情况 B: S2 还没 merge** (在 S2 branch 之上开):
```bash
cd i:\VsCodeWorkPlaces\Wxgzh
git worktree add .worktrees/v2-foundation-s3 -b v2-foundation/s3 v2-foundation/s2
cd .worktrees/v2-foundation-s3
pnpm install
cd apps/server && pnpm test  # 应 ≥ 155/155 PASS
```

### 3.2 读 S3 plan,提取 task

读 `docs/superpowers/plans/2026-06-02-v2-foundation-s3-observability.md` 完整内容,提取所有 task (类似 S2 的 21 task)。

### 3.3 用 subagent-driven-development 推进

按 S2 经验:
- 1 个 task 派 1 个 implementer subagent
- spec + code quality review 必跑
- 优先 batch 2-3 task / subagent
- 每个 task 1 commit, 报 commit SHA
- 偏离 plan 时如实报告, 严格按 V1 实际调整

---

## 4. V1 实际状态 (S2 实测后, 避免重新探查)

### 4.1 已装依赖 (V1 已有, S3 不要再装)

- `zod@3.25` ✓ (S2 升)
- `@nestjs/swagger@7.4` ✓
- `@anatine/zod-openapi@2.2` ✓ (S2 加)
- `nestjs-zod@1.4` ✓ (S2 加)
- `winston@3.13` ✓ (V1 已有)
- `pino`? — **未在 V1**, S3 装之前 grep 确认 (plan 假设有, 实际未必)
- `nestjs/throttler@6` ✓
- `minio@8` ✓
- `bullmq@5` ✓
- `ioredis@5.4` ✓
- `helmet@8` ✓
- `class-validator` + `class-transformer` ✓ (V1 仍用, 跟 Zod 并存)

### 4.2 已有 infrastructure (S2 已摸清, 复用)

- 全局 `HttpExceptionFilter` (`apps/server/src/common/filters/http-exception.filter.ts`) — 透出 `errors` 字段
- 全局 `LoggingInterceptor` (`apps/server/src/common/interceptors/logging.interceptor.ts`) — V1 已有, **S3 改造候选**
- 全局 `TraceIdInterceptor` (`apps/server/src/common/interceptors/trace-id.interceptor.ts`) — V1 已有, **S3 改造候选**
- `TenantMiddleware` (`apps/server/src/common/middleware/tenant.middleware.ts`)
- 全局 `ValidationPipe` (class-validator, main.ts) — V1 + S2 Zod 并存
- `app.setGlobalPrefix('api/v1')`
- Swagger 在 `/api/docs` (S2 加了 zod-openapi extend)
- `common/contracts/` 18 个 contract 文件 (S2 资产)
- 3 装饰器 `@ZodBody` / `@ZodQuery` / `@ZodResponse`
- `common/pipes/zod-validation.pipe.ts` (ZodValidationPipe)

### 4.3 测试基线

- `pnpm test` → **155/155 PASS** (23 suites)
- 测试 helper 在 `apps/server/test/helpers/{prisma-test,factories,wechat-mock,auth-helper}.ts`
- coverage 阈值 60% (S1 设)

### 4.4 Prisma client 重新生成 (新 worktree 必须)

新 worktree 没装过 prisma, 第一次跑测试前:
```bash
cd i:/VsCodeWorkPlaces/Wxgzh/.worktrees/v2-foundation-s3
pnpm prisma generate
```

否则报 `Cannot find module '.prisma/client/default'`。

### 4.5 已知坑 (S2 经验)

- `tsconfig.tsbuildinfo` 是 build artifact, 改 jest 后会被 tsc 标记 dirty, **不要 stage** (V1 习惯)
- `app.module.ts` 注册 APP_GUARD/APP_FILTER/APP_INTERCEPTOR 的顺序敏感 (V1 已配齐)
- `globalPrefix('api/v1')` — controller 路径都带 /api/v1 前缀
- 微信 webhook `apps/server/src/integrations/wechat/webhook/...` 用 V1 inline type, S2 任务 19 ESLint 已豁免 (rule 不适用)
- `payment.controller.ts:145` 有 `as any` 类型断言 (V1 业务, 不动)
- 单独跑 `pnpm test swagger-sync` 可能因 BullModule 尝试连真实 Redis 卡住 — **用 `--forceExit`**: `pnpm test swagger-sync --forceExit`
- `pnpm test` 后 jest worker 可能不优雅退出 (timer leak 警告, 非阻塞)

---

## 5. S3 计划假设 vs V1 实际可能偏差 (S2 类比)

S2 计划假设 17 controller, 实际 15。S3 计划假设可能偏差:
- **OTel 集成**: 计划假设 V1 无 OTel, 实际**V1 确认无 OTel** (按 V2.0 spec §0 表)
- **Prometheus**: 计划假设 V1 无 metrics, **V1 确认无**
- **pino**: 计划假设装 pino, **V1 实际用 winston**, 需要 S3 决策: 全切 pino / 兼容 winston
- **Grafana**: 新增, 无 V1 历史
- **告警**: 新增

S3 implementer 推进时如发现"V1 已有 X" 或 "V1 没有 Y" 跟 plan 假设不同, **STOP 并报告**, 不要硬塞。

---

## 6. S3 不在本 handoff 范围的事项

- S1 push/merge 决策 (用户)
- S2 push/merge 决策 (用户)
- main 上是否先 fast-forward S1, 再单独 PR S2 (用户)
- S3 计划本身的修订 (用户在开 S3 前会更新)

---

## 7. 新会话 prompt 模板

新会话里直接粘:

```
启动 V2.0 S3「可观测性」。

读取: i:\VsCodeWorkPlaces\Wxgzh\.worktrees\v2-foundation-s2\docs\superpowers\handoffs\2026-06-02-s3-handoff.md
然后: 读 S3 plan: docs/superpowers/plans/2026-06-02-v2-foundation-s3-observability.md

按照 handoff 第 3 节开 v2-foundation/s3 worktree, 验证 baseline (≥155 tests), 提取所有 task,
用 subagent-driven-development 推进。

已知调整:
- V1 用 winston 不是 pino, S3 推进时如 plan 假设 pino 需确认决策
- 单独跑 swagger-sync 加 --forceExit 防 BullModule 卡住
- pnpm prisma generate 必跑 (新 worktree)
```

---

## 8. 变更记录

| 版本 | 日期 | 变更 | 作者 |
|---|---|---|---|
| v1.0 | 2026-06-02 | S2 完工时建立, 给 S3 启动用 | S2 session |
